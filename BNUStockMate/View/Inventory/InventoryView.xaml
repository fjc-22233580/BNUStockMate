<UserControl
    x:Class="BNUStockMate.View.Inventory.InventoryView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:inventory="clr-namespace:BNUStockMate.ViewModel.Inventory"
    xmlns:local="clr-namespace:BNUStockMate.View.Inventory"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DataContext="{d:DesignInstance inventory:InventoryViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>


        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,10"
            HorizontalAlignment="Center"
            FontSize="24"
            FontWeight="Bold"
            Text="Warehouse Inventory" />

        <TextBlock
            Grid.Row="1"
            Margin="10,0,0,0"
            FontWeight="Bold"
            Foreground="Red"
            Text="{Binding LowStockWarningMessage}"
            Visibility="{Binding IsLowStock, Converter={StaticResource BooleanToVisibilityConverter}}" />

        <ListView
            Grid.Row="2"
            Margin="10"
            ItemsSource="{Binding Inventory}">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding Sku}" Header="SKU" />
                    <GridViewColumn DisplayMemberBinding="{Binding Name}" Header="Name" />
                    <GridViewColumn DisplayMemberBinding="{Binding Description}" Header="Description" />
                    <GridViewColumn DisplayMemberBinding="{Binding Quantity}" Header="Quantity" />
                    <GridViewColumn DisplayMemberBinding="{Binding IsLowStock}" Header="Low Stock?" />
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</UserControl>
