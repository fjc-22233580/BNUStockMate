<UserControl
    x:Class="BNUStockMate.View.Contacts.ContactsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:contacts="clr-namespace:BNUStockMate.ViewModel.Contacts"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:BNUStockMate.View.Contacts"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DataContext="{d:DesignInstance contacts:ContactsViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,10"
            HorizontalAlignment="Center"
            FontSize="24"
            FontWeight="Bold"
            Text="Contacts Directory" />

        <Grid Grid.Row="1">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Row="0"
                Grid.Column="0"
                Margin="0,0,0,10"
                HorizontalAlignment="Center"
                FontSize="20"
                FontWeight="Bold"
                Text="Customers" />

            <ListView
                Grid.Row="1"
                Grid.Column="0"
                Margin="10"
                ItemsSource="{Binding Customers}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn DisplayMemberBinding="{Binding Id}" Header="Id" />
                        <GridViewColumn DisplayMemberBinding="{Binding Name}" Header="Name" />
                        <GridViewColumn DisplayMemberBinding="{Binding Phone}" Header="Phone" />
                        <GridViewColumn DisplayMemberBinding="{Binding Email}" Header="Email" />
                        <GridViewColumn DisplayMemberBinding="{Binding Address}" Header="Address" />
                    </GridView>
                </ListView.View>
            </ListView>

            <TextBlock
                Grid.Row="0"
                Grid.Column="1"
                Margin="0,0,0,10"
                HorizontalAlignment="Center"
                FontSize="20"
                FontWeight="Bold"
                Text="Suppliers" />

            <ListView
                Grid.Row="1"
                Grid.Column="1"
                Margin="10"
                ItemsSource="{Binding Suppliers}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn DisplayMemberBinding="{Binding Id}" Header="Id" />
                        <GridViewColumn DisplayMemberBinding="{Binding Name}" Header="Name" />
                        <GridViewColumn DisplayMemberBinding="{Binding Phone}" Header="Phone" />
                        <GridViewColumn DisplayMemberBinding="{Binding Email}" Header="Email" />
                    </GridView>
                </ListView.View>
            </ListView>

        </Grid>


    </Grid>
</UserControl>
